package com.octosign.whitelabel.error_handling;

public enum Code {
        DEFAULT,
        NOT_READY,
        UNSUPPORTED_OPERATION,
        ATTRIBUTE_MISSING,
        MALFORMED_INPUT,
        UNSUPPORTED_FORMAT,
        INVALID_SCHEMA,
        VALIDATION_FAILED,
        SIGNING_FAILED,
        RESPONSE_FAILED,
        NULL_ARGUMENT,
        DECODING_FAILED,
        TRANSFORMATION_ERROR,
        UNEXPECTED_ORIGIN,
        UNEXPECTED_ERROR
    }